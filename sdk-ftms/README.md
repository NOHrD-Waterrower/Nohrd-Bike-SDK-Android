# `com.nohrd.bike:sdk-ftms`

The `com.nohrd.bike:sdk-ftms` artifact contains the sources for
reading data from a connected FTMS (FiTness Machine Service) Bike.

## Usage

### Calibration

### Cycling data
